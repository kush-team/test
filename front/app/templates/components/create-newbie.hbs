<h2>Cargar Ficha</h2>
{{#if searching}}
	<p>Buscando...</p>
{{else}}
	{{#if voter}}
		
		<p>
			<a class="small-btn" {{action 'create'}}>Guardar Ficha</a>
			<a class="small-btn"  {{action 'clear'}}>Limpiar</a>
		</p>

		<p>{{voter.nombre}} {{voter.apellido}}</p>
		<p>{{voter.matricula}}</p>
		<p>{{voter.sexo}}</p>
		<p>{{voter.afiliado}}</p>
		<p>{{voter.domicilio}}</p>
		<p>{{voter.escuela}}</p>
		<p>{{voter.direccion_escuela}}</p>
		<p>{{voter.mesa}}</p>
		
	{{else}}
		{{#if controller.searched}}
			<p>No se encontro resultado</p>
			<a class="small-btn" {{action 'clear'}}>Limpiar</a>
		{{else}}
			{{partial 'partials/padron-form'}}
		{{/if}}
	{{/if}}
{{/if}}
